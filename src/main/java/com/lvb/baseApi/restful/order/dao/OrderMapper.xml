<?xml version="1.0" encoding="UTF-8" ?>

<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.lvb.baseApi.restful.order.dao.OrderMapper">

    <select id = "getOrderList" parameterType="java.util.HashMap" resultType="com.lvb.baseApi.restful.order.entity.OrderEntity">
        select * from app_order o where o.user_id = #{map.userId}
        <if test="map.stateCode!=null">
            and  o.interrogation_type = #{map.stateCode}
        </if>
        order by o.create_time desc
    </select>

    <select id = "getDoctorOrderList" parameterType="java.util.HashMap" resultType="com.lvb.baseApi.restful.order.entity.OrderEntity">
        select * from app_order o where  o.doctor_id in (4,5,6)
        <if test="map.stateCode!=null">
            and  o.interrogation_type = #{map.stateCode}
        </if>
        order by o.create_time desc
    </select>

</mapper>
